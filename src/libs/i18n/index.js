import ru from './ru.json';
import kk from './kk.json';
import en from './en.json';

const locales = {
    ru: {
        ...ru,
    },
    kk: {
        ...kk,
    },
    en: {
        ...en,
    }
}

export default locales;